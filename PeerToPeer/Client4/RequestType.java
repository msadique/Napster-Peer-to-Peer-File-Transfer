//enum RegisterType having some values related to the type of request client initiates.

public enum RequestType {
	Register,
	Unregister,
	RequestPeerList,
	RequestFile,
	DontRequestFile,
	RequestAllFile,
	RequestUpdate
}
